components:
  schemas:

    SendSmsRequest:
      type: object
      required:
        - chatId
        - text
      properties:
        chatId:
          type: string
        text:
          type: string

    SendSmsResponse:
      type: object
      required:
        - jobId
      properties:
        jobId:
          type: string
          format: uuid

