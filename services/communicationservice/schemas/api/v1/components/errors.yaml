components:
  schemas:
    ErrorCode:
      type: string
      enum:
        - MISSING_IDEMPOTENCY_TOKEN_HEADER
        - MISSING_AUTHORIZATION_TOKEN_HEADER
        - INVALID_AUTHORIZATION_TOKEN
        - MISSING_TEXT
        - INVALID_REQUEST_FORMAT
        - FOREIGN_KEY_VIOLATION
        - NOT_EXISTING_CHANNEL_TYPE
        - RESOURCE_NOT_FOUND
        - INTERNAL_ERROR
        - WEBSOCKET_CONNECTION_CANCELLED

    ErrorResponse:
      type: object
      required:
        - code
        - message
      properties:
        code:
          $ref: '#/components/schemas/ErrorCode'
        message:
          type: string

